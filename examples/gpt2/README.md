# GPT-2 inference demo

```
$ python3 demo.py --method naive
Peak memory: 3101.94 MiB

$ python3 demo.py --method lazy-loading
Peak memory: 246.31 MiB

$ python3 demo.py --method lazy-loading chunked-embedding
Peak memory: 51.94 MiB
```
